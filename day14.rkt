#lang racket

(define (fmt fn)
  (file->string fn))

(define (part1 input)
  input)

(define (part2 input)
  input)

(module+ test
  (require rackunit)
  (check-equal? '() '()))

(module+ main
  (displayln "TODO"))